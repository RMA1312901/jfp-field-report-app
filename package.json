{
  "name": "jfp-field-reports",
  "version": "1.0.0",
  "description": "JFP Enterprises Inc — Daily Field Report System",
  "private": true,
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "cd client && npm install && npx vite build",
    "start": "node server/index.js",
    "dev": "node --watch server/index.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "better-sqlite3": "^11.6.0",
    "cors": "^2.8.5",
    "resend": "^4.0.0",
    "helmet": "^8.0.0",
    "compression": "^1.7.5"
  }
}
